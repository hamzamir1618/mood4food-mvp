# ── Pre-built binary pins (install FIRST to avoid source builds) ──
numpy>=1.26.4

# ── Core API Framework ──
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart

# ── Graph Database Driver ──
neo4j==5.25.0

# ── Vector Store ──
# ChromaDB requires C++ build tools (MSVC) on Windows.
# For the MVP, Tier 2 uses a local stub vector store (no external dep).
# To enable full ChromaDB, install MSVC Build Tools and: pip install chromadb==0.5.15

# ── Data Handling ──
pydantic==2.9.2

# ── HTTP Client (internal service calls) ──
httpx==0.27.2

# ── Audio Transcription (Tier 1a — Whisper) ──
# Requires FFmpeg installed on the system (choco install ffmpeg / apt install ffmpeg)
openai-whisper
