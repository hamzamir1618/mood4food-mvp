# ── Python ──────────────────────────────────────────────────────────────────
.venv/
venv/
__pycache__/
*.pyc
*.pyo
*.pyd
*.py[cod]
*.egg-info/
dist/
build/
*.egg

# ── Node ────────────────────────────────────────────────────────────────────
node_modules/

# ── Vector Store (local runtime data) ───────────────────────────────────────
tier_2/vector_store.json
tier_2/chroma_store/

# ── Dynamic Contracts ───────────────────────────────────────────────────────
tier_1/contracts/*.json

# ── Uploads & Model Cache ───────────────────────────────────────────────────
uploads/*
!uploads/*.m4a
!uploads/*.jpg
*.gguf

# ── IDE / Editor ────────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*~
.project
.classpath
.settings/

# ── OS ──────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
Desktop.ini
ehthumbs.db

# ── Logs ────────────────────────────────────────────────────────────────────
*.log
*errors.md

# ── Environment / Secrets ───────────────────────────────────────────────────
.env
.env.local
.env.production
.env.*.local
# Credential files downloaded from a provider console. Aura drops one of these into
# the working directory, and this repository is public.
Neo4j-*.txt
*credentials*.txt
*-Created-*.txt

# ── Throwaway diagnostic scripts ────────────────────────────────────────────
scratch/

# ── Data review worksheets (contain dataset rows; see ATTRIBUTIONS.md before publishing) ──
data_review/

# ── Dataset (kept out of the public repository; see ATTRIBUTIONS.md) ────────
/handoff_output/
/data/

# ── Saved design canvases (they bundle the design editor; rebuild them from the .dc.html sources) ──
design/**/mood4food-*.html
